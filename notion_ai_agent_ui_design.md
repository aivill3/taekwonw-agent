# Notion 기반 AI Agent UI 설계안

## 1. 설계 목표

현재 뉴스 수집 → 선정 → 소주제 생성 → 승인 → 초안 생성 → 검수 파이프라인에서 가장 큰 UX 문제는 **사람이 승인해야 하는 단위와 데이터가 저장된 단위가 다르다는 것**이다.

현재는 뉴스 DB에서 뉴스별로 소주제를 확인하고 승인한다.

하지만 실제 업무 단위는 다음과 같다.

> "AI가 어떤 뉴스들을 하나의 콘텐츠로 묶고, 어떤 소주제로 블로그를 구성했는가?"

따라서 Notion을 단순한 결과 저장소가 아니라 **Human-in-the-loop AI Agent Workspace**로 사용하고, 승인 단위를 `News`에서 `Content`로 변경한다.

---

# 2. 전체 구조

```text
📁 AI CONTENT WORKSPACE

├── 📰 News DB
│      ↓
│   AI가 수집 / 분석 / 선정
│      ↓
├── 🎯 Content DB
│      ↓
│   AI가 뉴스 묶음 + 소주제 구성
│   사람이 여기서 승인
│      ↓
├── ✍️ Draft DB
│      ↓
│   AI 초안 생성
│   사람이 여기서 검수
│
└── 📊 Dashboard
```

Relation 구조:

```text
News DB
   │
   │ Relation
   ▼
Content DB
   │
   │ Relation
   ▼
Draft DB
```

핵심 데이터 모델:

- **News**: 인터넷에서 발견한 하나의 원문
- **Content**: AI가 하나의 블로그 콘텐츠로 구성한 단위
- **Draft**: Content를 바탕으로 생성된 실제 블로그 초안

예:

```text
뉴스 A ─┐
뉴스 B ─┼──→ Content #001 ──→ Draft #001
뉴스 C ─┘
```

또는:

```text
뉴스 A ─────→ Content #002 ──→ Draft #002
```

---

# 3. 핵심 UX 변경

| 현재 | 변경 |
|---|---|
| 뉴스 단위 승인 | **콘텐츠 단위 승인** |
| 여러 뉴스 페이지를 찾아 확인 | **Content 페이지에서 한 번에 확인** |
| 본문을 사이드 Peek으로 확인 | **AI 요약 + 핵심 내용 인라인 제공** |
| 승인 후 코드 수동 실행 | **승인 → Agent 자동 실행** |
| 소주제 4개가 News 컬럼에 존재 | **Content에 소주제 귀속** |

가장 중요한 변화는 다음이다.

> **News를 승인하지 않는다. Content를 승인한다.**

---

# 4. 📰 News DB

## DB 이름

`📰 News`

## 역할

인터넷에서 수집한 뉴스 원문과 분석 결과를 저장한다.

News DB는 사람이 최종 승인하는 작업 공간이 아니라 **Agent가 관리하는 원본/분석 데이터**에 가깝게 설계한다.

## Properties

| 컬럼명 | Notion 타입 | 용도 |
|---|---|---|
| 제목 | Title | 뉴스 제목 |
| URL | URL | 원문 링크 |
| 언론사 | Select | 언론사 |
| 수집일 | Date | 수집 시점 |
| 발행일 | Date | 원문 발행일 |
| 본문 | Text | 정제된 원문 |
| 요약 | Text | AI 요약 |
| 키워드 | Multi-select | 추출 키워드 |
| 점수 | Number | 뉴스 선정 점수 |
| 길이 | Number | 본문 글자 수 |
| 뉴스 상태 | Status | 수집/분석/선정 상태 |
| Content | Relation → Content | 연결된 콘텐츠 |
| 사용 여부 | Select | 미사용/사용 |
| 원문 ID | Text | 외부 시스템 ID |

## 뉴스 상태

```text
📥 수집됨
🔎 분석중
⭐ 선정됨
🔗 콘텐츠 연결됨
📝 작성 완료
📦 보관
```

Workflow:

```text
수집됨
  ↓
분석중
  ↓
선정됨
  ↓
콘텐츠 연결됨
  ↓
작성 완료
```

**News DB에는 승인 상태를 넣지 않는다.**

---

# 5. News DB View

사용자가 직접 확인할 때는 필요한 정보만 보이도록 한다.

### View: `⭐ 선정 뉴스`

Filter:

```text
뉴스 상태 = 선정됨
```

추천 컬럼:

```text
제목
언론사
발행일
점수
길이
Content
```

본문 전체를 테이블에 노출하지 않는다.

---

# 6. News 페이지 Template

News 페이지는 다음과 같이 구성한다.

```text
# {{제목}}

🔗 원문 보기

## AI 요약

{{요약}}

## 핵심 키워드

{{키워드}}

## 원문

{{본문}}
```

단, 승인자가 News DB를 돌아다니며 본문을 확인하는 UX는 없애는 것이 목표다.

실제 승인 과정에서는 Content 페이지에서 연결된 뉴스들의 요약과 핵심 내용을 함께 보여준다.

---

# 7. 🎯 Content DB

## DB 이름

`🎯 Content`

## 역할

**실제 사람의 승인 단위**다.

Content 하나가 최종적으로 블로그 하나가 된다.

예:

```text
Content #001
├── 뉴스 A
├── 뉴스 B
├── 뉴스 C
└── 뉴스 D
        ↓
   소주제 4개
        ↓
      승인
        ↓
    Draft #001
```

---

# 8. Content DB Properties

| 컬럼명 | Notion 타입 | 설명 |
|---|---|---|
| 콘텐츠명 | Title | 블로그 주제 |
| 상태 | Status | Agent workflow |
| 콘텐츠 유형 | Select | 단일 뉴스 / 복수 뉴스 |
| Source News | Relation → News | 소스 뉴스 |
| 뉴스 수 | Rollup | 연결 뉴스 개수 |
| 총 본문 길이 | Rollup | 전체 글자 수 |
| 콘텐츠 점수 | Number | AI 선정 점수 |
| 선정 이유 | Text | AI 분석 결과 |
| 소주제 1 | Text | 첫 번째 소주제 |
| 소주제 2 | Text | 두 번째 소주제 |
| 소주제 3 | Text | 세 번째 소주제 |
| 소주제 4 | Text | 네 번째 소주제 |
| 승인자 | Person | 승인한 사용자 |
| 승인일 | Date | 승인 시간 |
| 검수 메모 | Text | 사용자 의견 |
| Draft | Relation → Draft | 생성된 초안 |
| Agent Action | Select | Agent에게 요청할 작업 |
| 생성일 | Created time | 자동 |
| 수정일 | Last edited time | 자동 |

---

# 9. Content 상태값

추천 상태:

```text
🤖 생성 대기
🟡 승인 대기
🔴 수정 요청
🟢 승인
⚙️ 초안 생성중
📝 초안 검수
🔴 초안 수정 요청
✅ 최종 승인
🚀 발행 완료
❌ 제외
```

Workflow:

```text
생성 대기
   ↓
승인 대기
   ↓
┌──────────────┬──────────────┐
│              │              │
수정 요청      승인           제외
│              │
↓              ↓
AI 재생성      초안 생성
               ↓
            초안 검수
               ↓
        ┌──────┴──────┐
        │             │
    수정 요청       최종 승인
                      ↓
                    발행
```

---

# 10. Content Relation

Content의 `Source News` 속성을 News DB와 Relation으로 연결한다.

예:

```text
🎯 Content #002

Source News

☑ 삼성전자 HBM 공급 확대
☑ SK하이닉스 HBM 증설
☑ 엔비디아 신규 GPU
☑ TSMC 생산 확대
```

이렇게 하면 하나의 Content 페이지에서 관련 뉴스 전체를 확인할 수 있다.

---

# 11. Content 페이지의 핵심 구조

Content 페이지 하나만 보고 승인할 수 있도록 만든다.

```text
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 AI CONTENT #20260828-001
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

상태
🟡 승인 대기

콘텐츠 유형
복수 뉴스

소스 뉴스
4개

예상 글자 수
2,100자

──────────────────────────────

💡 AI가 선정한 이유

최근 24시간 동안 HBM 수요와
AI 반도체 경쟁에 관한 뉴스가
동시에 증가했습니다.

──────────────────────────────

📰 참고 뉴스 — 4개

① 삼성전자 HBM 공급 확대
   점수: 96

   AI 요약:
   삼성전자가 AI 반도체 수요에 대응하기 위해...

   [원문 열기]


② SK하이닉스 HBM 증설
   점수: 91

   AI 요약:
   SK하이닉스가...

   [원문 열기]


③ 엔비디아 신규 GPU
   점수: 88

④ TSMC 생산 확대
   점수: 84

──────────────────────────────

✍️ AI 추천 소주제

① HBM 시장이 커지는 이유

② 삼성전자와 SK하이닉스의 경쟁

③ 엔비디아 GPU와 HBM 수요

④ 글로벌 AI 반도체 시장 전망

──────────────────────────────

📝 검수 메모

[ 사용자 입력 ]

──────────────────────────────

🟢 승인
🔴 수정 요청
⚫ 제외

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

핵심은 **Content 페이지 하나에서 소스 뉴스, AI 분석, 소주제, 승인까지 모두 확인할 수 있게 하는 것**이다.

---

# 12. AI 선정 이유를 반드시 보여준다

단순히 소주제만 보여주는 것보다 AI가 왜 이 구조를 만들었는지 보여주는 것이 중요하다.

예:

```text
💡 AI 선정 이유

최근 24시간 동안 4개 뉴스에서
공통적으로 HBM 수요 증가가 반복적으로
언급되었습니다.

따라서 이 콘텐츠에서는

① 시장 변화
② 삼성전자 전략
③ 경쟁 구도
④ 향후 전망

순으로 구성했습니다.
```

이렇게 하면 승인자가 원문 전체를 읽고 AI의 판단을 다시 추론할 필요가 줄어든다.

---

# 13. 콘텐츠 유형

Content DB에 다음 Select를 둔다.

```text
단일 뉴스
복수 뉴스
```

### 단일 뉴스

```text
Content #001

콘텐츠 유형 = 단일 뉴스
Source News = 1개
소주제 = 4개
```

### 복수 뉴스

```text
Content #002

콘텐츠 유형 = 복수 뉴스
Source News = 4개
소주제 = 4개
```

현재의 뉴스 길이 기반 로직을 사용자에게도 명확하게 보여줄 수 있다.

---

# 14. 소주제 4개 컬럼

현재의:

```text
소주제1
소주제2
소주제3
소주제4
```

구조는 초기에는 유지하는 것을 추천한다.

이유:

- 표에서 빠르게 확인 가능
- 기존 Agent 로직을 크게 수정하지 않아도 됨
- 승인자가 4개 소주제를 한눈에 비교 가능

장기적으로 소주제가 복잡해지면 별도의 `Subtopics DB`로 분리할 수 있지만, 현재 단계에서는 Content DB에 유지하는 것이 단순하다.

---

# 15. 🟢 승인 Button

Notion Button을 이용해 승인 행동을 단순화한다.

승인 버튼을 누르면:

```text
Content 상태
    ↓
🟢 승인
```

그리고:

```text
승인자 = 현재 사용자
승인일 = 현재 시간
```

을 기록한다.

이후 Agent Backend가 상태 변경을 감지한다.

```text
Notion
   ↓
status = 승인
   ↓
FastAPI
   ↓
Draft Agent
   ↓
Gemini Flash
```

따라서 **승인자가 별도로 초안 작성 코드를 실행하지 않는다.**

---

# 16. 🔴 수정 요청 Button

사용자가 검수 메모에 내용을 입력한다.

예:

```text
📝 검수 메모

"3번 소주제가 너무 일반적입니다.
엔비디아의 신규 GPU와 HBM 수요의 관계를
좀 더 구체적으로 다뤄주세요."
```

그리고:

```text
🔴 수정 요청
```

을 누른다.

Agent:

```text
수정 요청 감지
      ↓
검수 메모 읽기
      ↓
Subject Agent
      ↓
소주제 재생성
      ↓
Notion 업데이트
      ↓
승인 대기
```

---

# 17. ⚫ 제외 Button

제외 버튼은 단순하게 처리한다.

```text
Content.status = 제외
```

Agent는 해당 Content를 더 이상 처리하지 않는다.

연결된 News는 별도로 보존하여 이후 다른 콘텐츠에 사용될 수 있다.

---

# 18. Agent Action

`Status`와 별도로 `Agent Action` 속성을 둔다.

Select 값:

```text
없음
소주제 재생성
초안 재생성
삽화 재생성
품질검사
발행
```

예:

```text
Agent Action
→ 삽화 재생성
```

이면 GPU Worker가 해당 작업을 처리한다.

---

# 19. Status와 Agent Action을 분리하는 이유

### Status

> 현재 콘텐츠가 Workflow의 어느 단계인가?

예:

```text
승인 대기
초안 검수
최종 승인
```

### Agent Action

> Agent에게 무슨 일을 시킬 것인가?

예:

```text
소주제 재생성
초안 재생성
삽화 재생성
```

둘을 섞지 않는 것이 중요하다.

---

# 20. ✍️ Draft DB

## DB 이름

`✍️ Draft`

## 역할

Content가 승인된 후 생성되는 실제 블로그 초안을 관리한다.

## Properties

| 컬럼명 | Notion 타입 | 설명 |
|---|---|---|
| 제목 | Title | 블로그 제목 |
| 상태 | Status | 초안 Workflow |
| Content | Relation → Content | 원본 Content |
| 본문 | Text | 블로그 초안 |
| 글자 수 | Number | 본문 길이 |
| 품질 점수 | Number | Quality Agent 결과 |
| 품질 상태 | Select | 통과 / 수정 |
| 삽화 | Files & media | 생성 이미지 |
| 삽화 수 | Number | 이미지 개수 |
| 수정 메모 | Text | 사용자 의견 |
| 승인자 | Person | 최종 승인자 |
| 승인일 | Date | 승인 시간 |
| 발행 URL | URL | 발행 후 URL |
| Agent Action | Select | Agent 작업 요청 |

---

# 21. Draft 상태

```text
⚙️ 생성중
📝 검수 대기
🔴 수정 요청
🔄 재생성중
🟢 승인
🚀 발행 완료
```

---

# 22. Draft 페이지 구조

Draft 페이지는 실제 블로그를 검수하는 화면처럼 구성한다.

```text
━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 삼성전자의 AI 반도체 전략
━━━━━━━━━━━━━━━━━━━━━━━━━━

상태
📝 검수 대기

원본 콘텐츠
🎯 AI 반도체 시장의 변화

──────────────────────────

# 삼성전자가 AI 반도체 시장에서
## 새로운 경쟁 구도를 만드는 이유

본문...

본문...

## HBM 시장의 변화

본문...

──────────────────────────

🎨 ILLUSTRATIONS

[ 이미지 1 ]

[ 이미지 2 ]

[ 이미지 3 ]

──────────────────────────

🤖 QUALITY CHECK

글자 수       2,031자   ✓
금칙어        0개       ✓
문체          해요체    ✓
RAG 근거      8개       ✓

──────────────────────────

📝 수정 메모

[                         ]

──────────────────────────

🔄 수정 요청

🟢 최종 승인

━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

# 23. Quality Agent 결과를 사용자에게 보여준다

기존 Python 자산인 품질 검사기를 활용하여 다음과 같이 표시한다.

```text
🤖 AI Quality

문체          ✓
금칙어        ✓
글자 수       ✓
사실관계      ✓
RAG 근거      ✓
소제목 구조   ✓
```

문제가 있다면:

```text
⚠️ 발견된 문제

- 1문단에서 해요체 규칙 위반
- 1,920자 → 목표 범위 정상
- 출처 없는 주장 1건
```

처럼 보여준다.

이렇게 하면 사람이 전체 글을 처음부터 다시 검수해야 하는 부담을 줄일 수 있다.

---

# 24. Dashboard

Dashboard는 별도의 DB가 아니라 **Linked Database View**로 구성한다.

첫 페이지:

```text
🤖 AI CONTENT WORKSPACE

━━━━━━━━━━━━━━━━━━━━━━

🟡 승인 대기

Content DB
Filter:
상태 = 승인 대기

→ Content #001
→ Content #002
→ Content #003


━━━━━━━━━━━━━━━━━━━━━━

📝 초안 검수

Draft DB
Filter:
상태 = 검수 대기

→ Draft #018
→ Draft #019


━━━━━━━━━━━━━━━━━━━━━━

🔴 수정 요청

Content DB
Filter:
상태 = 수정 요청

→ 2건

Draft DB
Filter:
상태 = 수정 요청

→ 1건
```

---

# 25. 추천 Dashboard 구조

첫 화면에서 사용자가 가장 먼저 처리해야 할 업무를 보여준다.

```text
🤖 AI CONTENT CENTER

오늘 처리할 작업

━━━━━━━━━━━━━━━━━━━━━━

🟡 SUBTOPIC APPROVAL

┌───────────────────────────────┐
│ AI 반도체 시장의 변화           │
│                               │
│ 뉴스 4개 · 예상 2,100자         │
│                               │
│ ① HBM 시장 변화                │
│ ② 삼성전자 경쟁 전략            │
│ ③ 엔비디아와 경쟁               │
│ ④ 글로벌 시장 전망              │
│                               │
│          [검토하기 →]          │
└───────────────────────────────┘


━━━━━━━━━━━━━━━━━━━━━━

📝 DRAFT REVIEW

┌───────────────────────────────┐
│ 삼성전자의 AI 반도체 전략       │
│                               │
│ 2,031자 · 품질검사 ✓            │
│                               │
│          [검토하기 →]          │
└───────────────────────────────┘
```

---

# 26. 전체 Workflow

현재 파이프라인을 다음과 같이 변경한다.

```text
뉴스 수집
 ↓
News DB
 ↓
점수 계산
 ↓
AI Ranking
 ↓
Content 후보 자동 생성
 ↓
┌──────────────────────────┐
│     Content Approval     │
│                          │
│ 뉴스 4개                  │
│ 소주제 4개                 │
│ AI 선정 이유               │
│ 뉴스 요약                  │
│                          │
│       [승인]              │
└──────────┬───────────────┘
           ↓
       Draft Agent
           ↓
┌──────────────────────────┐
│       Draft Review       │
│                          │
│ 본문                       │
│ 삽화                       │
│ Quality 결과              │
│                          │
│      [최종 승인]           │
└──────────┬───────────────┘
           ↓
         발행
```

사람이 해야 하는 핵심 작업은:

1. Content 승인
2. Draft 최종 검수/승인

두 단계로 단순화된다.

---

# 27. Content Agent의 역할

현재의 뉴스 길이 기반 소주제 생성 로직은 `Content Agent`의 역할로 분리하는 것이 좋다.

```text
News Agent
  │
  ├─ 뉴스 수집
  ├─ 중복 제거
  ├─ 본문 추출
  ├─ 정제
  ├─ 키워드 분석
  └─ 점수 계산
        ↓
Content Agent
  │
  ├─ 뉴스 길이 분석
  ├─ 관련 뉴스 클러스터링
  ├─ 단일/복수 뉴스 결정
  ├─ 콘텐츠 주제 생성
  └─ 소주제 4개 생성
        ↓
Human Approval
```

예:

```text
긴 뉴스 A
   ↓
단일 뉴스형 Content
   ↓
소주제 4개
```

또는:

```text
짧은 뉴스 B ┐
짧은 뉴스 C ├→ 복수 뉴스형 Content
짧은 뉴스 D ┘
             ↓
          소주제 4개
```

---

# 28. 최종 Notion DB 관계

```text
┌───────────────────────────┐
│         📰 NEWS           │
│                           │
│ 제목                      │
│ URL                       │
│ 언론사                    │
│ 발행일                    │
│ 본문                      │
│ 요약                      │
│ 키워드                    │
│ 점수                      │
│ 길이                      │
│ 뉴스 상태                 │
│                           │
│ Content ──────────────┐   │
└───────────────────────│───┘
                        │
                        │ Relation
                        ▼
┌───────────────────────────┐
│       🎯 CONTENT          │
│                           │
│ 콘텐츠명                  │
│ 상태                      │
│ 콘텐츠 유형               │
│ Source News ◀─────────────┘
│ 소주제1                   │
│ 소주제2                   │
│ 소주제3                   │
│ 소주제4                   │
│ 선정 이유                 │
│ 검수 메모                 │
│ Agent Action              │
│                           │
│ Draft ────────────────┐   │
└───────────────────────│───┘
                        │
                        │ Relation
                        ▼
┌───────────────────────────┐
│         ✍️ DRAFT          │
│                           │
│ 제목                      │
│ 본문                      │
│ 상태                      │
│ 품질 점수                 │
│ 품질 결과                 │
│ 삽화                      │
│ 수정 메모                 │
│                           │
│ Content ◀─────────────────┘
└───────────────────────────┘
```

---

# 29. Agent와 Notion의 역할 분리

Notion을 Agent의 실행 엔진으로 만들지는 않는다.

Notion은 **Human Collaboration Layer**로 사용한다.

```text
Supabase
    ↓
AI가 관리하는 Machine State
```

```text
Python Agent
    ↓
실제 AI 작업 실행
```

```text
Notion
    ↓
사람이 읽고 / 수정하고 / 승인하는 Workspace
```

즉:

- Supabase = Machine State
- Python = Agent Brain
- FastAPI = Agent API
- Queue = 작업 관리
- GPU Worker = 삽화 생성
- Notion = Human Workspace
- Slack = Notification

---

# 30. 최종 Agent Workflow

```text
                    NEWS
                      │
             수집 / 정제 / 랭킹
                      │
                      ▼
               ┌────────────┐
               │ AI Selection│
               └─────┬──────┘
                     │
                     ▼
              CONTENT CANDIDATE
                     │
            ┌────────┴────────┐
            │                 │
       단일 뉴스          복수 뉴스
            │                 │
       4개 소주제       여러 뉴스 → 4개 소주제
            │                 │
            └────────┬────────┘
                     ▼
               🧑 HUMAN REVIEW
                     │
             ┌───────┼────────┐
             │       │        │
           승인    수정요청    제외
             │       │
             │       └→ Agent 재생성
             ▼
              DRAFT AGENT
                     │
                     ▼
                QUALITY AGENT
                     │
                     ▼
              ILLUSTRATION AGENT
                     │
                     ▼
               🧑 HUMAN REVIEW
                     │
                     ▼
                  PUBLISH
```

---

# 31. 핵심 설계 원칙

## 원칙 1. 승인 단위를 News에서 Content로 바꾼다

현재:

> 뉴스 A를 승인할까?

변경:

> AI가 뉴스 A~D를 이 관점으로 묶어 하나의 블로그로 만드는 것이 타당한가?

---

## 원칙 2. Content 페이지 하나에서 판단을 끝낸다

승인자는 다음을 한 페이지에서 본다.

- 연결 뉴스
- 뉴스 점수
- 뉴스 요약
- AI 선정 이유
- 소주제 4개
- 예상 분량
- 검수 메모
- 승인 / 수정 요청 / 제외

---

## 원칙 3. 승인 후 코드를 직접 실행하지 않는다

현재:

```text
소주제 승인
 ↓
사용자가 코드 실행
 ↓
초안 생성
```

변경:

```text
소주제 승인
 ↓
Agent 자동 감지
 ↓
초안 생성
```

---

## 원칙 4. Status와 Agent Action을 분리한다

Status:

> Workflow의 현재 상태

Agent Action:

> Agent에게 요청할 작업

---

## 원칙 5. Notion은 사람을 위한 UI로 사용한다

Notion이 모든 Machine State를 담당하도록 만들지 않는다.

AI 실행 상태, Job, Prompt Version, Model, Token, Error 등은 별도의 Backend/DB에서 관리하고, 사용자가 필요한 결과만 Notion에 노출한다.

---

# 32. 최종적으로 기대하는 사용자 경험

기존:

```text
뉴스 목록 확인
 ↓
뉴스 A 페이지 열기
 ↓
본문 확인
 ↓
뉴스 B 찾기
 ↓
뉴스 B 페이지 열기
 ↓
본문 확인
 ↓
뉴스 C 찾기
 ↓
...
 ↓
각 뉴스 승인 체크
 ↓
초안 코드 실행
```

변경:

```text
🤖 AI CONTENT WORKSPACE
        ↓
🟡 승인 대기 Content
        ↓
Content 페이지 열기
        ↓
[뉴스 4개 + 요약 + AI 선정 이유 + 소주제 4개]
        ↓
🟢 승인
        ↓
Agent 자동 초안 생성
        ↓
📝 Draft 검수
        ↓
🟢 최종 승인
        ↓
발행
```

결론적으로 **Notion을 버릴 필요가 없다.**

오히려 현재 시스템에서는 Notion의 강점인:

- 초대 기반 협업
- 자유로운 편집
- 댓글
- 멘션
- 페이지 공유
- 익숙한 문서 UX

를 그대로 살리면서,

**`News → Content → Draft` 3단계 데이터 모델과 Content 단위 승인 Workflow를 도입하는 것이 가장 큰 UX 개선 포인트다.**

